import torch
import pandas as pd

from torch.utils.data import Dataset
from typing import Optional, Dict, Any, Callable

class PrimeData(Dataset):
    def __init__(self):
        pass

    def __len__(self) -> int:
        pass

    def __getitem__(self, idx: int) -> Dict[str, Any]:
        pass


